export type CaseFiltersState = {
  topic: string;
  company: string;
  year: string;
  tag: string;
};

export type CaseFilterOptions = {
  topics: string[];
  companies: string[];
  years: string[];
  tags: string[];
};
